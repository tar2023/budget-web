import { MobileFormatPipe } from './mobbile-format.pipe';

describe('MobbileFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MobileFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
